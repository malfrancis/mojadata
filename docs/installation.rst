.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install mojadata

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mojadata
    $ pip install mojadata
